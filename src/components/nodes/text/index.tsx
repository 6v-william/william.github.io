import './index.less';
export default (props: any) => {
  const { text } = props;
  const propsMap = {
    fontSize: '14px',
    color: '#000',
  };
  const stylePropNameMap = {
    fontSize: 'font-size',
    color: 'color',
    fontFamily: 'font-family',
  };
};
